MESSAGE(STATUS "CURRENT CMAKE_MODULE_PATH IS ${CMAKE_MODULE_PATH}")
FIND_PACKAGE(HELLO REQUIRED)
IF(HELLO_FOUND)
	ADD_EXECUTABLE(hello main.c)
	INCLUDE_DIRECTORIES(${HELLO_INCLUDE_DIR})
	TARGET_LINK_LIBRARIES(hello ${HELLO_LIBRARY})
ENDIF(HELLO_FOUND)

