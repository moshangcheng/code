message(STATUS "Before Add Executable, WIN32: ${WIN32}")
message(STATUS "Before Add Executable, MSVC: ${MSVC}")

# add_executable(test test.c)

message(STATUS "After Add Executable, WIN32: ${WIN32}")
message(STATUS "After Add Executable, MSVC: ${MSVC}")

